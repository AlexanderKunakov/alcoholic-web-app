import React from 'react';
import {Container, Typography} from "@mui/material";

const ErrorPage = () => {
    return (
        <Container>
            <Typography align={"center"} variant={"h5"}>Oops! An Error occurred.</Typography>
        </Container>
    );
};

export default ErrorPage;
